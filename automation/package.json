{
  "name": "auction-app-automation",
  "version": "1.0.0",
  "description": "Auction App API Testing",
  "main": "auction_flow.js",
  "scripts": {
    "test": "jest --verbose --detectOpenHandles --forceExit",
    "test:watch": "jest --watchAll --verbose",
    "flow": "node auction_flow.js"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "chalk": "^4.1.2",
    "dotenv": "^16.4.1",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12"
  },
  "jest": {
    "testTimeout": 30000,
    "setupFilesAfterEnv": ["./jest.setup.js"]
  }
}
